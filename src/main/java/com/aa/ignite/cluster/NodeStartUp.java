
package com.aa.ignite.cluster;

//import com.aa.rpt.ignite.model.FCST;
//import com.aa.rpt.ignite.model.FcOutput;
//import com.aa.rpt.util.RptCli;
import org.apache.ignite.Ignite;
//import org.apache.ignite.IgniteCache;
import org.apache.ignite.IgniteException;
import org.apache.ignite.Ignition;

/**
 * Starts up an empty node with example compute configuration.
 */
public class NodeStartUp {
    /**
     * Start up an empty node with example compute configuration.
     *
     * @param args Command line arguments, none required.
     * @throws IgniteException If failed.
     */
    //private static final String CACHE_NAME = "FCSTEmbeded";

    public static void main(String[] args) throws IgniteException {

//        Ignite ignite = Ignition.start(conFileName);
        //Ignition.start("C:\\Users\\855239\\gridgain-enterprise-fabric-8.3.4\\gridgain-enterprise-fabric-8.3.4\\examples\\config\\example-ignite.xml");
//        ignite.getOrCreateCache(cacheName);
    }
}
